# Scientific Computing Dependencies
name: cortex_decoder_env
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3
  - numpy
  - pandas
  - matplotlib
  - scikit-learn
  - pip
  - pip:
    - -r requirements.txt